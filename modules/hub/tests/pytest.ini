# pytest.ini
[pytest]
log_level=INFO
pythonpath=/
testpaths=tests
asyncio_mode=auto
asyncio_default_fixture_loop_scope=function
markers = 
    e2e: mark tests as end-to-end tests
    integrate: mark tests as integration tests
    unit: mark tests as unit tests
addopts = "--tb=line"